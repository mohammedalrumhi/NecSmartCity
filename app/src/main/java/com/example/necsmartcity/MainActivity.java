package com.example.necsmartcity;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.layout_home);
  /// we start codding smart city in the middle of the dat
        // at this point w are starting codding the layout that avavliable to user

        //this screen is to user that allow to user to login with the application.



    }


}
